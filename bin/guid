#!/usr/bin/env python

import os

import sys
sys.path.insert(
    0, os.path.abspath(os.path.join(os.path.dirname(__file__), "..")))

from api.utils import ids

uid = ids.generate(length=4)
print("%s" % (uid))
